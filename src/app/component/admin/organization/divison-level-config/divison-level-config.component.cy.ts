import { DivisonLevelConfigComponent } from './divison-level-config.component'

describe('DivisonLevelConfigComponent', () => {
  it('should mount', () => {
    cy.mount(DivisonLevelConfigComponent)
  })
})