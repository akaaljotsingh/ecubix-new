import { PageListBroadcastComponent } from './page-list-broadcast.component'

describe('PageListBroadcastComponent', () => {
  it('should mount', () => {
    cy.mount(PageListBroadcastComponent)
  })
})